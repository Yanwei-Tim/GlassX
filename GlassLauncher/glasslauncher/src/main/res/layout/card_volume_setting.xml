<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#cc000000">

    <ProgressBar
        android:id="@+id/progress_volume"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:progressDrawable="@drawable/alert_progressbar"
        android:layout_marginLeft="@dimen/height_setting_maginTop"
        android:layout_marginRight="@dimen/height_setting_maginTop"
        android:layout_marginBottom="@dimen/height_setting_maginTop"
        android:layout_alignParentBottom="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <ImageView
        android:id="@+id/img_volume"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_volume_0_large" />

</RelativeLayout>