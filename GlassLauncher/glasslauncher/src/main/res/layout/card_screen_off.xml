<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl_screen_off_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <!-- 左侧图标 -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="5"
            android:background="@color/setting_left"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/img_screen_main"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/icon_screen_off" />
        </LinearLayout>
        <!-- 右侧文字 -->

        <LinearLayout
            style="@style/Layout_Setting"
            android:layout_weight="3"
            android:orientation="vertical">

            <TextView
                android:id="@+id/txt_title"
                style="@style/Text_Setting"
                android:text="@string/label_setting_screen_off" />

            <TextView
                android:id="@+id/txt_screen_off"
                style="@style/Text_Setting"
                android:layout_marginTop="@dimen/height_setting_spacingTop"
                android:text="15秒"
                android:textColor="@color/color_green" />
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>